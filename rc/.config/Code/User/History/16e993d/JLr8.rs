use proconio::input;

fn main() {
   // input
   input! {
      n: usize,
      mut a: i32,
      b: i32,
      s: String,
   }
   // solve
   let ans = 0;
   a += b;
   let ent = Vec::new();
   for i in 0..n {
      if ent.len() <= a {
         if s[i]
      }
   }
   // answer
   println!("{}", ans);
}
