use proconio::input;

fn main() {
   // input
   input! {
      n: i32,
      a: i32,
      b: i32,
      s: String,
   }
   // solve
   let ans = 0;
   // answer
   for i in ans.len() {
      println!("{}", ans);
   }
}
