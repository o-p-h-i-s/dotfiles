use proconio::input;

fn main() {
    // input
    input! {
       n: usize,
       a: i32,
       b: i32,
       s: String,
    }
    // solve
    let ans = 0;
    // answer
    println!("{}", ans);
}
